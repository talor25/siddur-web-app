# 🤖 תיעוד הפרומפטים והשיחות עם כלי ה-AI (AI Prompts Log)
## פרויקט: סידור עדות המזרח – אפליקציית תפילה דינמית

מסמך זה מרכז את כל השאלות, ההנחיות והפרומפטים (Prompts) שהופנו לכלי ה-AI במהלך תכנון, אפיון, פיתוח, ניפוי שגיאות והעלאת הפרויקט ל-GitHub במסגרת **קורס הכרות עם כלי בינה מלאכותית**.

---

## 📌 חלק 1: שלב אפיון המוצר והגדרת הדרישות (Product Specification & Planning Phase)

### פרומפט #1: הגדרת חזון האפליקציה ודרישות העיצוב
* **הפרומפט שנשאל:**
  > "אני רוצה ליצור אפליקציית סידור תפילה בנוסח עדות המזרח. האפליקציה צריכה להיות רספונסיבית (Mobile-First), בעיצוב יוקרתי של ספר קודש מודרני, עם תמיכה במצב לילה/יום, בקרת גודל גופן, ומצבים הלכתיים דינמיים שמשנים את טקסט התפילה (עונת קיץ/חורף, עשרת ימי תשובה)."
* **תוצר ה-AI:**
  * הגדרת ארכיטקטורת Frontend נרקי (Vanilla HTML5/CSS3/JavaScript).
  * יצירת העיצוב החזותי (CSS Variables, Flexbox, Dark Mode).
  * בניית מנגנון הראוטינג מבוסס Hash ומבנה הנתונים הראשוני ב-`prayers-data.js`.

---

## 📌 חלק 2: שלב ניפוי שגיאות ותיקון באגים (Debugging & Error Fixing)

### פרומפט #2: אבחון ותיקון שגיאת טעינה בקובץ `prayers-data.js`
* **הפרומפט שנשאל:**
  > "בדוק את הפרוייקט משהו גורם לשגיאה ואני לא מצליח לטעון את ה-JS בשם prayers data. תקן לי את הבאג כדי שהסידור יעבוד ויציג את התפילות כשאני בוחר תפילה."
* **תוצר ה-AI:**
  * ביצוע אבחון מעמיק בקוד וזיהוי שגיאת תחביר (Tagged Template Literal call: `amida `...`` שהחזירה `TypeError: amida is not a function`).
  * תיקון שרשור המחרוזות לפורמט תקין (`${amida}`).
  * השלמת קטעי טקסט שנקטעו ("אמת שאתה הוא...", "גאל ישראל", וכותרת "וידוי ותחנון").

---

## 📌 חלק 3: שלב הרחבת הפונקציונליות (Feature Expansion & Navigation)

### פרומפט #3: הוספת תוכן עניינים וקטעי ניווט לתפילות
* **הפרומפט שנשאל:**
  > "עדכנתי את התפילות וכעת כל התפילות מלאות בקובץ התפילות. אשמח שתוסיף לי קטעי ניווט בתפילת מנחה ותפילת ערבית וברכת המזון."
* **תוצר ה-AI:**
  * הוספת תיוגי מזהים (`id`) לקטעי התפילות ב-`js/prayers-data.js` (כגון `mincha-aleinu`, `arvit-aleinu`, `hamazon-harachaman`).
  * עדכון אובייקט `PrayerNavSections` ב-`js/app.js` המאפשר מעבר מהיר וגלילה חלקה דרך תפריט הניווט הנפתח (`📖`).

---

## 📌 חלק 4: שלב התיעוד ומסמכי ההגשה (Documentation Phase)

### פרומפט #4: יצירת מסמך אפיון מוצר מפורט בעברית
* **הפרומפט שנשאל:**
  > "הפרויקט שלי כבר מוכן להעלאה לגיטהאב. אבל לפני כן אשמח שתיצור לי מסמך איפיון של המוצר בעברית ותשים לי אותו בתיקיית הפרויקט."
* **תוצר ה-AI:**
  * יצירת הקובץ `PRODUCT_SPECIFICATION.md` בתיקיית הפרויקט, הכולל תיאור מנהלי, אפיון UI/UX (Mobile-First), אפיון פונקציונלי, ארכיטקטורה טכנולוגית, תהליך הפיתוח והוראות הרצה.
  * יצירת קובץ `README.md` מעוצב עבור GitHub.

---

## 📌 חלק 5: שלב העלאה והפצה (GitHub Setup & Deployment)

### פרומפט #5: הכנת המאגר המקומי והעלאה ל-GitHub
* **הפרומפט שנשאל:**
  > "אתה יכול להעלות עבורי את הפרויקט לחשבון הגיטהאב שלי?"
* **תוצר ה-AI:**
  * אתחול מאגר Git מקומי (`git init`), יצירת `.gitignore`.
  * ביצוע הקומיטים (Commits) בענף `main`.
  * מתן הנחיות ברורות לקישור המאגר ב-GitHub וביצוע Push.

---

## 📌 חלק 6: שלב תיעוד השיח להגשת העבודה

### פרומפט #6: יצירת מסמך מרכז של שאלות הפרויקט
* **הפרומפט שנשאל:**
  > "אשמח שתכין לי טקסט להעתקה לקובץ md על השאלות ששאלתי אותך למען הפרויקט."
* **תוצר ה-AI:**
  * יצירת מסמך זה (`AI_PROMPTS_LOG.md`) המרכז בצורה נקייה ומאורגנת את כל שלבי העבודה והשיח עם הבינה המלאכותית.

---

**נערך ונכתב לצורך הגשת עבודת סוף הקורס | הכרות עם כלי בינה מלאכותית**
